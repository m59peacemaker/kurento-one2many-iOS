var StateRouter = require('abstract-state-router');
var ReactStateRenderer = require('react-state-renderer');

var stateRouter = new StateRouter(ReactStateRenderer({
  activeClass: 'active'
}), document.body);

module.exports = stateRouter;

