var config = {
  port: 8080,
  dirs: {
    broadcasts: __dirname+'/tmp/broadcasts',
  },
  kmsUri: 'ws://localhost:8888/kurento',
  video: {
    format: {
      record: 'webm',
      playback: 'mp4'
    }
  }
};

module.exports = config;
